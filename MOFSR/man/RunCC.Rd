% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCC.R
\name{RunCC}
\alias{RunCC}
\title{Run Consensus Clustering using ConsensusClusterPlus}
\usage{
RunCC(
  data,
  maxK = 6,
  reps = 1000,
  pItem = 0.8,
  pFeature = 1,
  clusterAlg = "hc",
  distance = "euclidean",
  title = "Consensus Clustering",
  plot = TRUE
)
}
\arguments{
\item{data}{A numeric matrix or data frame where rows represent features and columns represent samples.}

\item{maxK}{The maximum number of clusters to evaluate (default: 6).}

\item{reps}{Number of subsamples (default: 1000).}

\item{pItem}{Proportion of items to sample (default: 0.8).}

\item{pFeature}{Proportion of features to sample (default: 1).}

\item{clusterAlg}{The clustering algorithm to use, either "hc" for hierarchical or "km" for k-means (default: "hc").}

\item{distance}{The distance metric to use, "pearson", "spearman", "euclidean", etc. (default: "euclidean").}

\item{title}{Optional title for the results (default: "Consensus Clustering").}

\item{plot}{Whether to plot the consensus matrix and dendrogram (default: TRUE).}
}
\value{
A list containing the consensus clustering results.
}
\description{
This function performs consensus clustering on the given data using the ConsensusClusterPlus package.
}
\details{
This function leverages the ConsensusClusterPlus package to perform consensus clustering, which is useful for identifying robust clusters in genomic data or other high-dimensional data.
}
\examples{
# Example usage:
data <- matrix(rnorm(1000), nrow = 100, ncol = 10)
result <- RunCC(data, maxK = 4)
}
\author{
Zaoqu Liu; Email: liuzaoqu@163.com
}
